#include <stdio.h>
int main()
{
    double salary[5];
    int i,sum=0;
    printf("Enter salary= ");
    for(i=0;i<5;i++)
        scanf("%d",&salary[i]);{
    for(i=0;i<5;i++)
        printf("\nsalary is %d\n",salary[i]);
        sum+= sum+salary[i];
        printf("sum= %d",sum+);}
return 0;
}
