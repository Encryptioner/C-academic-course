#include <stdio.h>>
int main()
{
    int i,sum=0;
    char str[10],c[5];
    for(i=0;;i++)
    {
        gets(str);
        c[]="61";
        if(str==61)
            printf("sum= %d\n",sum);
        else
            sum=sum+str;
    }
}
