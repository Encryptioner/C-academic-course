#include <stdio.h>
int main()
{
    int a,b;
    printf("Enter value of a&b= ");
    scanf("%d %d", &a , &b);
    if(a>b)
    {
      printf("a is maximum than b");
    }
    else
    {
      printf("b is maximum than a");
    }
return 0;
}
